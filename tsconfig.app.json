{
	"extends": "./tsconfig.json", // Path to base configuration file to inherit from.
  "include": ["src/**/*.ts", "examples/**/*"], // Include all TypeScript files in the src, and examples directory.
  "exclude": ["node_modules", "__tests__/**/*"] // Exclude node_modules, examples, and __tests__ directories.
}
